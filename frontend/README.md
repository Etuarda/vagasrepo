# Vagas.io — Frontend (HTML + JS)

## Rodando local
Você pode servir essa pasta com qualquer servidor estático.

Exemplos:

### Python
```bash
python -m http.server 5500
```

Acesse: `http://localhost:5500`

## Configuração da API
Edite `src/js/config.js` para apontar para sua API.
